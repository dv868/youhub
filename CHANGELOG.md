# 更新日志

## 2026-09-01

> 本项目从 LINUX DO 社区起步，感谢佬友们的内容与启发。以下为当天全部改动。

### 新增

- **自动测速（后台执行）**：更新后发现新渠道含免费（未标价）或 ≤0.01x 超低价模型时，弹窗询问是否自动实测；测速在服务器后台线程串行执行，**刷新/关闭页面不中断**；页面顶部显示蓝色进度横幅（`⚡ 后台测速中 3/7`），完成后自动刷新结果。每渠道最多测 3 个模型，单次上限 15 个。
- **「🔍 检查更新」按钮**：轻量更新模式，只采集水位线以上的新增渠道，跳过全量体检。实测无新渠道时 0.4 秒完成（完整版约 14 秒），日常随手查用轻量版，cron 定时任务仍跑完整版负责剔除下架渠道。
- **搜索框支持 cid**：原「搜索渠道名」升级为「搜索渠道名 / cid」，支持精确匹配（输入 21942）和前缀匹配（输入 219 列出 219xx 全部渠道），名称模糊匹配保持不变。

### 修复

- **「更新全部」超时**：全量体检（384 渠道逐个查 HUB 确认存活）原为串行 + 每 20 个 sleep 1 秒，仅体检就需 3~4 分钟，超过接口 120 秒超时必挂。改为 6 线程并发（实测 384 渠道约 13 秒），接口超时提到 300 秒，并加并发锁防重复触发。
- **体检误删保护（三态判定）**：原逻辑"查询失败 = 判死"，JWT 失效时会把全部渠道误判死亡并清空数据（曾实际发生过一次）。改为三态：enabled=保留；明确返回非 enabled 或查无此渠道=剔除；**网络/JWT/GraphQL 异常=未知，保留不动**。
- **手机端价格与测速按钮消失**：移动端 `.models` 仍继承桌面端 `min-width:400px`，390px 屏幕下模型列整体溢出，价格和「测」按钮被推出屏幕。已清除该最小宽度，渠道信息改为纵向堆叠，「发布公网」「更新全部」等管理按钮空间不足时自动换行。390px 真机宽度截图回归通过。

### 调整

- **只展示活渠道**：页面默认只显示 `status == enabled` 且未被确认下架的渠道；单渠道「刷新」时若 HUB 已查不到或状态非 enabled，立即从页面数据剔除（死渠道归档 `dead_channels.json`）。此前为全量展示 + 标记下架。
- **品牌**：站名 YOUHUB；顶栏标语 `YouHub. Your Hub`；数据更新时间从右上角 ISO 格式改为统计行右侧 `09-01 15:03` 可读格式。
- **部署安全**：公网 Cloudflare Workers 改为只从 `public/` 白名单目录发布（曾可访问到 `.git/` 与源码）；新增 `_headers`（no-cache + HSTS/XFO/XCTO/Referrer-Policy）；从仓库移除含账号 ID 的 `.wrangler/` 缓存。
- **仓库迁移**：新仓库为本仓库（youhub），干净单提交历史；旧仓库已删除。

### 已知限制

- Anthropic 协议渠道（type=anthropic）无法用 OpenAI 格式测速，显示 ✗ 失败，不会误报成功；多数 anthropic 渠道在 HUB 网关有 OpenAI 兼容层可测。
- 测速依赖管理员凭证；凭证 24 小时有效，失效后采集/展示不受影响，仅测速与单渠道刷新暂停。

### 数据口径

- 收录 cid 20012+ 渠道；每天 3 次定时巡检（08:30 / 12:30 / 17:30）+ 全量体检；价格为渠道主申报口径；测速为维护者真实调用结果（速度 + 时间戳），仅供参考。
